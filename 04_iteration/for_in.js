//**********************for in***********************

const myObject  = {
    JS: 'Javascript',
    CPP: 'C++',
    JAVA: 'Java'
}

for (const key in myObject) {
        // console.log(`${key} full form is ${myObject[key]}`);
        
    }

const languages = ["PY", "CPP", "JAVA", "JS", "C"]

for (const key in languages) {
    console.log(languages[key]);
}  

